import { StyleSheet } from 'react-native'
import vars from '../src/shared-styles/variables'

export default StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    alignItems: 'stretch',
    backgroundColor: vars.appBackgroundColor,
  },
})
